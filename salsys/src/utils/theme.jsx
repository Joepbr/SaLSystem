import { createTheme } from '@mui/material/styles'

const theme = createTheme({
    palette: {
        mode: 'dark',
        primary: {
            main: '#25254b',
        },
        secondary: {
            main: '#9d2f2e'
        }
    }
})

export default theme